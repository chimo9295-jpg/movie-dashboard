# 2026五一档电影数据看板 — 项目分析报告

> 基于实际代码的逐模块分析，回答数据获取、存储、预测、可视化、ChatBI 及整体架构相关问题。

---

## 一、数据获取

### 1.1 猫眼数据怎么获取的？

数据源：**猫眼专业版** (`piaofang.maoyan.com`)，两条路径互补：

**路径 A — 电影详情页** (`/movie/{movieId}`)

页面内嵌一个 `<script type="application/json">` 的 JSON 数据块（`crawler_complete_fixed.py:80-84`）：

```python
for script in soup.find_all('script', {'type': 'application/json'}):
    d = json.loads(script.string)
    if isinstance(d, dict) and 'boxshowChartData' in d:
        json_data = d
        break
```

其中的 `boxshowChartData.chartData.box` 包含：
- `date[]` — 每日日期列表
- `real[]` — 每日分账票房（万元）

这是**明文数据**，不需要字体解码，是日票房的主要来源。

**路径 B — Dashboard API** (`/dashboard-ajax`)

返回电影列表 + 实时指标（上座率、排片占比、票房占比、综合/分账累计票房描述）。但部分数字使用**自定义 woff 字体编码**，数字 0-9 被替换为 PUA（Private Use Area）字符如 `&#xed4f;`（`mayday_crawler.py:30-31`）。

**关键计算逻辑**（`mayday_crawler.py:490-505`）：

```
raw_split_cumulative  = 详情页分账累计 (不含预售、不含服务费)
pre_sale_box          = Dashboard分账累计 - raw_split_cumulative
service_fee_rate      = (Dashboard综合累计 - Dashboard分账累计) / Dashboard分账累计
日综合票房            = 日分账票房 × (1 + service_fee_rate)
累计综合票房          = 预售基准 × (1 + service_fee_rate) + Σ(日综合票房)
```

### 1.2 豆瓣数据怎么获取的？

使用第三方 API `justoneapi.com` 的豆瓣代理接口：

| 脚本 | 接口 | 获取内容 |
|------|------|----------|
| `douban_rating_to_supabase.py` | `/api/douban/get-subject-detail/v1` | 豆瓣评分 (`douban_score`)、评分人数 (`douban_votes`) |
| `douban_reviews_to_supabase.py` | `/api/douban/get-movie-reviews/v1` | 影评列表（分页），含作者、评分、标题、内容、时间 |

豆瓣 API 有**费用控制**（`douban_reviews_to_supabase.py:25-27`）：
- 每日最多 3 次 API 调用（`DAILY_CALL_LIMIT = 3`）
- 请求间隔至少 60 秒（`REQUEST_INTERVAL = 60`）
- 本地 JSON 文件记录调用次数，超限自动停止
- 按 `comment_text` 的 MD5 hash 去重，避免重复存储

### 1.3 有没有遇到反爬？怎么处理的？

猫眼有**两层反爬**：

| 反爬机制 | 原理 | 处理方式 |
|----------|------|----------|
| **Woff 字体编码** | 数字被替换为 PUA 区不可见字符，页面渲染时用自定义字体映射回数字 | 写了一个 `MaoyanFontDecoder` 类（`mayday_crawler.py:133-212`），下载 woff 文件，用 `fontTools.ttLib` 解析 cmap 表，通过 3 种策略（glyph 名称匹配 → PUA 码点偏移 → 末位 hex 匹配）映射回 0-9 |
| **SignKey 签名** | POST 接口需要 MD5 签名：拼接 `method + timeStamp + UA + index + channelId + sVersion + key` 后 MD5 | 分析了 `veri.ts` 的签名算法（key=`A013F70DB97834C0A5492378BD76C53A`），但实际爬取中**避开了**需要签名的接口，只用不需要签名的 GET 页面 |
| **IP 封禁** | 高频云服务器 IP 触发验证码或封禁 | 使用 BrightData 住宅代理绕过 |

### 1.4 BrightData 是什么，为什么用它？

**BrightData** 是一个商业住宅代理网络（residential proxy network）。调用方式（`mayday_crawler.py:109`）：

```bash
brightdata scrape "url" --format raw
```

选它的原因：
- 猫眼对数据中心 IP（云服务器）会触发验证码或直接封禁
- BrightData 提供的是**真实住宅 IP**，请求看起来像普通用户浏览器访问
- 自带重试机制（最多 3 次，间隔 5s/10s/15s 递增，超时 120s）
- 返回原始字节（`--format raw`），保留数字编码字符的原始形式以便后续解码

---

## 二、数据存储

### 2.1 Supabase 表结构

**4 张核心表**（`mayday_schema.sql`）：

| 表名 | 用途 | 关键字段 | 唯一约束 |
|------|------|----------|----------|
| `mayday_movies` | 电影基本信息 | `movie_id`, `movie_name`, `release_date` | `movie_id` UNIQUE |
| `mayday_daily_stats` | 日票房 + 累计票房（每天每部一行） | `movie_id`, `stat_date`, `daily_box`, `total_box` | `(movie_id, stat_date)` UNIQUE |
| `mayday_dashboard` | 上座率 / 排片占比 / 票房占比快照 | `movie_id`, `crawl_date`, `avg_seat_view`, `box_rate`, `show_count_rate` | `(movie_id, crawl_date)` UNIQUE |
| `douban_comments` | 豆瓣影评 | `comment_text`, `rating_text`, `author`, `crawl_date` | 无 DB 约束（通过 `comment_text` MD5 业务去重） |

另外 `movie_realtime` 表（在 `douban_rating_to_supabase.py` 中使用，存储评分历史快照，不在 `mayday_schema.sql` 中定义，属历史遗留差异）。

**关联方式**：通过 `movie_id` 关联电影维度，通过 `stat_date` / `crawl_date` 关联时间维度。属于星型结构 — `mayday_movies` 是维度表，其余是事实表。

前端查询示例（`index.html:222-228`）：
```javascript
const [movies, dailyStats, dashboard, realtime, comments] = await Promise.all([
    sbFetch('/mayday_movies?order=movie_id.asc'),
    sbFetch('/mayday_daily_stats?order=stat_date.asc&limit=200'),
    sbFetch('/mayday_dashboard?order=movie_id.asc&limit=10'),
    sbFetch('/movie_realtime?order=crawl_time.asc&limit=100'),
    sbFetch('/douban_comments?limit=100'),
]);
```

### 2.2 为什么选 Supabase 而不是本地数据库？

1. **前端直连** — 静态 HTML 通过 Supabase REST API 直接查询数据（`index.html:197-206`），不需要后端中间层，部署到 Vercel 零服务器成本
2. **免运维** — 不需要管理 PostgreSQL 服务器、备份、高可用
3. **免费额度** — 适合个人项目的数据量（几十到几百行数据）
4. **自带 REST API** — `{url}/rest/v1/{table}` 自动生成 CRUD 端点，支持 `select`、`order`、`eq` 等查询参数
5. **实时订阅**（预留能力） — 数据结构本身支持未来升级为实时 WebSocket 推送

---

## 三、分析与预测

### 3.1 预测模型原理

#### EWMA（指数加权移动平均）

核心公式：
```
S_t = α × X_t + (1-α) × S_{t-1}
```

- α=0.3：新数据权重 30%，历史趋势权重 70%
- 相比简单移动平均，EWMA 对**近期变化更敏感**，同时保持平滑性
- 适合电影票房这种"趋势延续性较强但每天有波动"的时间序列
- 计算量极小，适合前端实时运算

#### 季节调整 EWMA（本次优化新增）

在原始 EWMA 基础上增加了**乘法季节分解**：

```
Step 1: 日期分类 → 假期(五一5/1-5/5)、周末(周六日)、工作日
Step 2: 计算各类型日均票房 → 假期均值、周末均值、工作日均值
Step 3: 计算季节系数 → 工作日基准=1.0x, 周末=周末均值/工作日均值, 假期=假期均值/工作日均值
Step 4: 去季节化 → Box_adj = Box_raw / 季节系数(对应类型)
Step 5: EWMA 平滑 → S_t = α × Box_adj_t + (1-α) × S_{t-1}
Step 6: 预测 → Pred_raw = α × Box_adj_last + (1-α) × S_last
Step 7: 重新季节化 → Pred_final = Pred_raw × 季节系数(明天类型)
```

生命周期判断基于**去季节化后的真实趋势**（`index.html:343-353`）：
```
上映 ≤ 5天 且 去季节化趋势 > -5%  → 爆发期
去季节化趋势 > +3%               → 增长期
去季节化趋势 > -8%               → 稳定期
去季节化趋势 ≤ -8%               → 衰退期
```

这里的关键是：工作日相对假期的自然票房回落 ≈ -5% 到 -15%，但只有当去季节化后的趋势真实下降超过 8% 时才判定为衰退，避免误判。

#### 服务端版本（`server.py:90-121`）

还有一个滚动窗口预测版本，特点：
- window=5 天，forecast=7 天
- 指数衰减权重（越近权重越大）
- 内置周末调整因子：周末 1.15x、工作日 0.92x
- 预测值加入序列用于迭代预测下一天

### 3.2 预测结果准吗？怎么验证？

验证方式（`data_cleaning.py:85-95`）：
```python
PRE_SALE = 1342.8  # 预售基准
running = PRE_SALE
for row in data:
    running += row["daily_box"]
    expected = round(running, 2)
    actual = row.get("total_box")
    if abs(expected - actual) > 1.0:  # 超过1万元偏差则告警
        issues.append(f"累计计算={expected}, 实际total_box={actual}")
```

这是通过**数据一致性反向验证**：累计票房理论上 = 预售 + Σ(日票房)，任何超过 1 万元的偏差说明数据有问题，会自动修复 `total_box` 字段。

**当前不足**：没有做系统性的预测准确率回溯验证（如 MAPE、RMSE）。可以用前 N 天真实数据预测第 N+1 天，与真实值对比计算误差。

---

## 四、可视化

### 4.1 ECharts 图表类型一览

| 图表 | ECharts 类型 | DOM ID | 说明 |
|------|-------------|--------|------|
| 累计票房 & 评分人数对比 | **双 Y 轴折线图** (`line` + `yAxisIndex`) | `chart-dual` | 左轴万元/亿，右轴评分人数万，双量纲对比 |
| 四部电影日票房对比 | **多条折线图** (`line`) | `chart-daily-comp` | 4 种颜色区分，symbol=circle，比较每日排名 |
| 累计票房对比 | **面积图** (`line` + `areaStyle` + `LinearGradient`) | `chart-accum` | 半透明渐变填充，突出体量差距 |
| 综合实力雷达图 | **雷达图** (`radar`) | `chart-radar` | 五维：日票房 / 累计票房 / 上座率 / 排片占比 / 票房占比 |

### 4.2 选型逻辑

- **时间序列** → 折线图（最直观感知趋势变化，smooth 平滑减少噪点）
- **多维对比** → 雷达图（一个画面看清各电影优劣势）
- **累计对比** → 面积图（渐变填充让"量级差距"一目了然）
- **汇总明细** → 表格（精确数字，支持条形内衬可视化排名）

所有图表配置了统一的深色主题（`backgroundColor: 'transparent'`，半透明配色匹配面板背景）。

---

## 五、ChatBI — AI 数据分析助手

### 5.1 DeepSeek API 接入流程

**前端版本**（`index.html:769-833`）：

```
用户输入问题
  → buildDataContext() 拼接 Markdown 格式的全量数据表格（含评分、各电影票房、最近7天明细）
  → 拼入 System Prompt："你是 BI 数据分析助手，用分点、表格回答，信息不足明确说明"
  → POST https://api.deepseek.com/v1/chat/completions
     model: deepseek-chat, temperature: 0.2, max_tokens: 2048
  → 返回的 Markdown 内容用 marked.js 渲染为 HTML
  → 追加到对话界面
```

API Key 存储在浏览器 `localStorage`，首次使用弹窗输入。

**服务端版本**（`server.py:150-258`）：

一个更工程化的实现，走 SQL 生成路线：
```
用户问题
  → System Prompt 告知 SQLite 表结构和约 10 条约束
  → DeepSeek 返回 JSON: {"sql": "...", "explanation": "...", "chart_type": "bar/line/pie/table/text", "chart_config": {...}}
  → 在 SQLite 内存数据库执行 SQL
  → 返回查询结果 + 图表配置
```

### 5.2 本地回退机制

当 DeepSeek API 不可用时（未配 Key 或网络错误），`server.py:202-247` 有 5 种关键词匹配回退：
- "好评/差评/占比" → 评分分布饼图
- "评分趋势/分数走势" → 豆瓣评分折线图
- "票房趋势/每日票房" → 日票房折线图
- "评论/影评/最新" → 最新影评表格
- 默认 → 票房趋势折线图

### 5.3 费用控制

豆瓣 API 有精细的费用管控（`douban_reviews_to_supabase.py:33-115`）：
- 每日调用上限 3 次
- 请求间隔 ≥ 60 秒
- 本地 `api_call_cache.json` 记录次数，跨天自动重置
- 启动时检查今日是否已有数据（避免重复调用）

---

## 六、整体评价

### 6.1 这个项目最难的部分是什么？

1. **猫眼字体反爬破解**（`mayday_crawler.py:133-212`）  
   woff 字体将 0-9 数字映射到 PUA 区的不可见字符，需要理解字体文件结构，通过 `fontTools.ttLib` 解析 cmap 表，写了 3 种回退策略来恢复数字映射。一旦猫眼前端改了编码方式，就得重新分析。

2. **综合票房 vs 分账票房的换算**（`mayday_crawler.py:490-505`）  
   猫眼详情页给的是分账票房（不含服务费），Dashboard 给的是综合票房（含服务费）。需要从两者的差值反推预售 + 服务费率，再用这些参数把所有日期的综合票房逐个算出来。逻辑不复杂但容易搞混单位（万元/亿元）。

3. **预测模型避免误判**  
   区分"周末/假期结束后的自然票房回落"和"真正的市场衰退"需要加日期效应。刚完成的季节调整优化就是解决这个问题：先剥除日历效应再判断趋势，再还原为具体日期的预测值。

### 6.2 如果重做会改什么？

1. **前端 Key 暴露** — Supabase publishable key 和 DeepSeek key 硬编码在 JS/Python 中。应加 BFF 层（`server.py` 已写了但没有被前端用起来），或用环境变量 + 构建时注入。

2. **预测模型加入回溯验证** — 用历史数据做 walk-forward validation，计算 MAPE，给出"预测置信度"而非裸数字。

3. **爬虫定时化** — 当前需手动执行 Python 脚本。应用 GitHub Actions cron 或 Vercel Cron Jobs 每天自动跑。

4. **字体解码改用 OCR** — woff 字体解析太脆弱。渲染后用 OCR 识别数字更鲁棒，不依赖前端内部编码规律。

5. **表结构统一** — `mayday_daily_stats`（爬虫）和 `movie_daily_stats`（server.py）两个表名混用，是历史遗留问题，应统一。

6. **日票房数据增加预测置信区间** — 不是给一个单点值，而是给一个范围（如 "明日预测: 800-950 万，置信度 80%"），更有实用价值。

7. **数据更新加版本记录** — 每次爬取记录来源 URL、数据质量标记、异常告警，方便追溯问题。

---

## 附录：文件清单

| 文件 | 功能 |
|------|------|
| `index.html` | 前端看板（ECharts 图表 + 季节调整 EWMA 预测 + ChatBI） |
| `mayday_crawler.py` | 猫眼五一档爬虫（字体解码 + 预售/服务费计算 + Supabase 存储） |
| `crawler_complete_fixed.py` | 猫眼单电影爬虫（给阿嬷的情书，日票房/累计票房） |
| `douban_rating_to_supabase.py` | 豆瓣评分获取 → Supabase `movie_realtime` 表 |
| `douban_reviews_to_supabase.py` | 豆瓣影评获取（含费用控制 + MD5 去重）→ Supabase `douban_comments` 表 |
| `mayday_schema.sql` | Supabase 建表 SQL（4 张核心表 + 索引） |
| `data_cleaning.py` | 数据清洗：检测 NULL 列 / 异常值 / total_box 一致性 / 重复数据 |
| `server.py` | Flask 后端（滚动窗口预测 + ChatBI SQL 生成 + 本地回退） |
| `vercel.json` | Vercel 部署配置（静态站点） |
| `test_all.py` | 测试脚本 |
| `requirements_crawler.txt` | Python 依赖：requests, beautifulsoup4, fonttools, supabase, numpy, flask |

---

*分析日期：2026-05-20 · 基于当前 `main` 分支实际代码*
